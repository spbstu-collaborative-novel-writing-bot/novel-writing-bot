= Collaborative Novel Writing Bot HTTP API
:doctype: book
:toc: left
:source-highlighter: highlightjs

== GET /healthcheck

Публичный endpoint проверки состояния приложения.

include::{snippets}/healthcheck/http-request.adoc[]
include::{snippets}/healthcheck/http-response.adoc[]
include::{snippets}/healthcheck/response-fields.adoc[]

== GET /users

Административный endpoint со списком зарегистрированных пользователей. Требует заголовок `X-Admin-Token`.

=== Успешный запрос

include::{snippets}/users-success/http-request.adoc[]
include::{snippets}/users-success/request-headers.adoc[]
include::{snippets}/users-success/http-response.adoc[]
include::{snippets}/users-success/response-fields.adoc[]

=== Ошибка доступа

include::{snippets}/users-forbidden/http-request.adoc[]
include::{snippets}/users-forbidden/http-response.adoc[]
include::{snippets}/users-forbidden/response-fields.adoc[]

== GET /admin

HTML-панель администратора. Панель запрашивает `X-Admin-Token` и вызывает защищенные JSON endpoint-ы.

== Admin JSON API

Все endpoint-ы требуют заголовок `X-Admin-Token`.

* `GET /admin/api/overview` — агрегированная статистика пользователей, романов, глав, участников и LLM-запросов.
* `GET /admin/api/users` — пользователи, роли и количество доступных романов.
* `GET /admin/api/novels` — романы, число авторов, глав и объем текста.
* `GET /admin/api/llm-requests` — последние LLM-запросы и ошибки.

== Mini App LLM API

Все endpoint-ы требуют заголовок `X-Telegram-Init-Data`.

* `POST /mini/api/chapters/{novelId}/{chapterId}/llm` — создает LLM-запрос из Web-редактора.
* `GET /mini/api/llm/{requestId}` — возвращает статус и результат LLM-запроса.
