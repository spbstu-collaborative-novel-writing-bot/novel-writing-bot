= Collaborative Novel Writing Bot HTTP API
:doctype: book
:toc: left
:source-highlighter: highlightjs

== GET /healthcheck

Публичный endpoint проверки состояния приложения.

include::{snippets}/healthcheck/http-request.adoc[]
include::{snippets}/healthcheck/http-response.adoc[]
include::{snippets}/healthcheck/response-fields.adoc[]

== GET /admin

HTML-оболочка панели администратора. Панель запрашивает `X-Admin-Token` в браузере и вызывает защищенные JSON endpoint-ы `/admin/api/*`.

== Admin JSON API

Все endpoint-ы требуют заголовок `X-Admin-Token`.

* `GET /admin/api/overview` — агрегированная статистика пользователей, романов, глав, участников и LLM-запросов.
* `GET /admin/api/users` — пользователи и количество доступных романов.
* `GET /admin/api/novels` — романы, число авторов, глав и объем текста.
* `GET /admin/api/llm-requests` — последние LLM-запросы и ошибки.
* `POST /admin/api/novels/{novelId}/delete` — удаляет произведение после обязательной отправки причины и полного `.txt` всем авторам.

=== GET /admin/api/users

include::{snippets}/admin-users-success/http-request.adoc[]
include::{snippets}/admin-users-success/request-headers.adoc[]
include::{snippets}/admin-users-success/http-response.adoc[]
include::{snippets}/admin-users-success/response-fields.adoc[]

== Mini App LLM API

Все endpoint-ы требуют заголовок `X-Telegram-Init-Data`; подпись и свежесть `auth_date` проверяются сервером.

* `POST /mini/api/chapters/{novelId}/{chapterId}/llm` — создает LLM-запрос из Web-редактора.
* `GET /mini/api/llm/{requestId}` — возвращает статус и результат LLM-запроса.
